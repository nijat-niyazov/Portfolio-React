import React from 'react';
import { GoogleMap, useJsApiLoader, Marker } from '@react-google-maps/api';

const Map = () => {
  const center = { lat: 40.389, lon: 49.803 };

  return <div>Map</div>;
};

export default Map;
